<Window x:Class="SoloReq.Views.UserAgentPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:SoloReq.Converters"
        Title=">_ Выбор User-Agent"
        Width="700" Height="500"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}"
        WindowStyle="None" AllowsTransparency="false"
        ResizeMode="CanResizeWithGrip">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" ResizeBorderThickness="6"
                      GlassFrameThickness="0" CornerRadius="0"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>

        <Style x:Key="FilterTabButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="8,6,8,8"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="FontFamily" Value="{StaticResource MonoFont}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="Transparent">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="2"/>
                            </Grid.RowDefinitions>
                            <ContentPresenter Grid.Row="0"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                            <Border x:Name="Indicator" Grid.Row="1" Height="2" Visibility="Collapsed">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#FF479C" Offset="0"/>
                                        <GradientStop Color="#FF9248" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="Tag" Value="Active">
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                                <Setter TargetName="Indicator" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource AccentPinkBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title bar -->
        <Grid Grid.Row="0" Background="{DynamicResource WindowBackgroundBrush}">
            <StackPanel Orientation="Horizontal" Margin="12,0" VerticalAlignment="Center">
                <TextBlock FontFamily="{StaticResource MonoFont}" FontSize="14" FontWeight="Bold">
                    <TextBlock.Foreground>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#FF479C" Offset="0"/>
                            <GradientStop Color="#FF9248" Offset="1"/>
                        </LinearGradientBrush>
                    </TextBlock.Foreground>
                    &gt;_
                </TextBlock>
                <TextBlock Text=" Выбор User-Agent" FontFamily="{StaticResource MonoFont}" FontSize="13"
                           Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
            </StackPanel>
            <Button Content="✕" Click="CloseClick" HorizontalAlignment="Right"
                    FontSize="12" Width="40" Height="32"
                    Background="Transparent" BorderThickness="0"
                    Foreground="{DynamicResource TextPrimaryBrush}" Cursor="Hand"
                    WindowChrome.IsHitTestVisibleInChrome="True"/>
        </Grid>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Filters -->
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="BtnFilterAll" Grid.Column="0" Content="// Все" Click="FilterAll_Click"
                        Style="{StaticResource FilterTabButton}" Tag="Active" Margin="0,0,4,0"/>
                <Button x:Name="BtnFilterDesktop" Grid.Column="1" Content="// Desktop" Click="FilterDesktop_Click"
                        Style="{StaticResource FilterTabButton}" Margin="0,0,4,0"/>
                <Button x:Name="BtnFilterMobile" Grid.Column="2" Content="// Mobile" Click="FilterMobile_Click"
                        Style="{StaticResource FilterTabButton}" Margin="0,0,4,0"/>
                <Button x:Name="BtnFilterCustom" Grid.Column="3" Content="// Пользовательские" Click="FilterCustom_Click"
                        Style="{StaticResource FilterTabButton}" Margin="0,0,8,0"/>

                <Button Grid.Column="4" Content="+ Добавить" Click="AddCustom_Click"
                        FontFamily="{StaticResource MonoFont}" FontSize="13"
                        Background="Transparent" BorderThickness="0"
                        Cursor="Hand" Padding="8,6" Margin="0,0,8,0">
                    <Button.Foreground>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#FF479C" Offset="0"/>
                            <GradientStop Color="#FF9248" Offset="1"/>
                        </LinearGradientBrush>
                    </Button.Foreground>
                </Button>

                <TextBox Grid.Column="5" x:Name="SearchBox"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         FontFamily="{StaticResource MonoFont}" FontSize="12"
                         Background="{DynamicResource EditorBackgroundBrush}"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         CaretBrush="{DynamicResource TextPrimaryBrush}"
                         BorderBrush="{DynamicResource BorderBrush}" Padding="8,4"
                         VerticalContentAlignment="Center" MinWidth="120"
                         Tag="Поиск..."/>
            </Grid>

            <!-- DataGrid -->
            <DataGrid Grid.Row="1" x:Name="UaDataGrid"
                      ItemsSource="{Binding FilteredEntries}"
                      SelectedItem="{Binding SelectedEntry}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False" IsReadOnly="True"
                      Style="{DynamicResource BlogDataGrid}"
                      BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}"
                      FontSize="11"
                      MouseDoubleClick="DataGrid_MouseDoubleClick"
                      KeyDown="DataGrid_KeyDown">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="4,0">
                                    <Button Content="✎" Click="EditCustom_Click" Style="{StaticResource ActionButton}"
                                            Margin="0,0,2,0" ToolTip="Редактировать"/>
                                    <Button Content="✕" Click="DeleteCustom_Click" Style="{StaticResource ActionButton}"
                                            ToolTip="Удалить"
                                            Visibility="{Binding IsCustom, Converter={StaticResource BoolToVisibility}}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Браузер" Binding="{Binding Browser}" Width="*"/>
                    <DataGridTextColumn Header="Версия" Binding="{Binding BrowserVersion}" Width="Auto"/>
                    <DataGridTextColumn Header="ОС" Binding="{Binding Os}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Status bar: preview -->
            <TextBlock Grid.Row="2" Margin="0,8,0,0"
                       FontFamily="{StaticResource MonoFont}" FontSize="11"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       Text="{Binding SelectedEntry.Ua, FallbackValue='Выберите User-Agent из списка'}"
                       TextTrimming="CharacterEllipsis"/>
        </Grid>
    </Grid>
</Window>
